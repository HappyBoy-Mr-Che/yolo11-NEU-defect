# ==================== 深度学习框架 ====================
# CPU 环境直接安装; GPU 机器请按 https://pytorch.org 选择对应 CUDA 版本
torch>=2.0
torchvision>=0.15

# ==================== 改进版 ultralytics (本地定制 8.3.2) ====================
# 不要用 pip 安装官方 ultralytics 覆盖, 请以开发模式安装仓库内定制版:
#   cd ultralytics && pip install -e .

# ==================== GUI ====================
PyQt5>=5.15

# ==================== 推理 / 导出 ====================
onnx>=1.16
onnxruntime>=1.17
onnxscript>=0.1      # torch>=2.9 的 torch.onnx.export 必需
onnxslim>=0.1.34     # ONNX 模型简化 (可选, 缺失时导出自动跳过该步骤)

# ==================== 数据处理 / 可视化 ====================
numpy
opencv-python>=4.8
Pillow
pyyaml
matplotlib

# ==================== 自定义模块依赖 ====================
einops
